import { Node } from 'node-red';
import { ConnectionSettings } from './core';
export interface ConfigNode extends Node {
    settings: ConnectionSettings;
    credentials: {
        username?: string;
        password?: string;
    };
}

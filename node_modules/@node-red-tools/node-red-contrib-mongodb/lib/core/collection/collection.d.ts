import { Collection as DbCollection, CollectionCreateOptions, Db, DbCollectionOptions, IndexSpecification } from 'mongodb';
import { MethodDescription } from './method';
export interface CollectionSettings {
    name: string;
    options?: DbCollectionOptions;
    autoCreate?: boolean;
    autoCreateOptions?: CollectionCreateOptions;
    autoCreateIndexes?: IndexSpecification[];
}
export declare class Collection {
    private __instance;
    static create(db: Db, collection: CollectionSettings): Promise<Collection>;
    static methods(): MethodDescription[];
    constructor(instance: DbCollection);
    get name(): string;
    execute<TIn = any, TOut = any>(methodName: string, args: TIn): Promise<TOut>;
}

import { Collection } from 'mongodb';
export declare type Method = (collection: Collection, args: any) => Promise<any>;
export interface MethodSignature {
    input: string[];
}
export interface MethodDescription {
    name: string;
    signature: MethodSignature;
}
export declare function list(): MethodDescription[];
export declare function resolve(name: string): Method | undefined;

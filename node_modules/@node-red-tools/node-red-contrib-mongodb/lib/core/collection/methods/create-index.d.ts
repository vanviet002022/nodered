import { Collection, IndexOptions } from 'mongodb';
export interface Arguments {
    spec: any;
    options?: IndexOptions;
}
export declare const createIndexSignature: Readonly<{
    input: string[];
}>;
export declare function createIndex(collection: Collection, args: Arguments): Promise<void>;

import { Collection, FilterQuery, ReplaceOneOptions } from 'mongodb';
export interface Arguments {
    query: FilterQuery<any>;
    replacement: any;
    options?: ReplaceOneOptions;
}
export declare const replaceOneSignature: Readonly<{
    input: string[];
}>;
export declare function replaceOne(collection: Collection, args: Arguments): Promise<string>;

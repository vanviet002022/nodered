import { Collection, FilterQuery, ReadPreferenceOrMode } from 'mongodb';
export interface Arguments {
    key: string;
    query?: FilterQuery<any>;
    options?: {
        readPreference?: ReadPreferenceOrMode;
        maxTimeMS?: number;
    };
}
export declare const distinctSignature: Readonly<{
    input: string[];
}>;
export declare function distinct(collection: Collection, args: Arguments): Promise<number>;

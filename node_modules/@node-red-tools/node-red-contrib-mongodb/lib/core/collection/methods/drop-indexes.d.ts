import { Collection } from 'mongodb';
export interface Arguments {
    options?: {
        maxTimeMS?: number;
    };
}
export declare const dropIndexesSignature: Readonly<{
    input: string[];
}>;
export declare function dropIndexes(collection: Collection, args: Arguments): Promise<void>;

import { Collection, FilterQuery, FindOneOptions } from 'mongodb';
export interface Arguments {
    query: FilterQuery<any>;
    options?: FindOneOptions;
}
export declare const findSignature: Readonly<{
    input: string[];
}>;
export declare function find(collection: Collection, args: Arguments): Promise<any[]>;

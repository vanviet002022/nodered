import { Collection, IndexSpecification } from 'mongodb';
export interface Arguments {
    spec: IndexSpecification[];
}
export declare const createIndexesSignature: Readonly<{
    input: string[];
}>;
export declare function createIndexes(collection: Collection, args: Arguments): Promise<void>;

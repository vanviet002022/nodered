import { Collection, CommonOptions } from 'mongodb';
export interface Arguments {
    name: string;
    options?: CommonOptions & {
        maxTimeMS?: number;
    };
}
export declare const dropIndexSignature: Readonly<{
    input: string[];
}>;
export declare function dropIndex(collection: Collection, args: Arguments): Promise<void>;

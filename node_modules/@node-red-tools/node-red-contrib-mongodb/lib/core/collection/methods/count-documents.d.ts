import { Collection, FilterQuery, MongoCountPreferences } from 'mongodb';
export interface Arguments {
    query?: FilterQuery<any>;
    options?: MongoCountPreferences;
}
export declare const countDocumentsSignature: Readonly<{
    input: string[];
}>;
export declare function countDocuments(collection: Collection, args: Arguments): Promise<number>;

import { Db, MongoClient, MongoClientOptions } from 'mongodb';
import { Collection, CollectionSettings } from './collection';
export declare class Connection {
    private __client;
    private __db;
    private __collections;
    static open(uri: string, collections: CollectionSettings[], options?: MongoClientOptions): Promise<Connection>;
    constructor(client: MongoClient, db: Db, collections?: Collection[]);
    isConnected(): boolean;
    db(): Db;
    collection(name: string): Collection;
    close(): Promise<void>;
    private __assertIsOpen;
}
